# 三级候选检索更新与实测

本文件保留近似模式的历史实验；当前默认已开启完整召回补查，最新结果与语义见 `CERTIFIED_REPORT.md`。复现本文件近似模式需显式设置 `verification="none"`。

## 结果

本次实现了长段粗检索、中段筛选和点位精定位。默认 12/16 配置在本批 70 次普通查询中全部低于 50 ms，比同批精确树的中位耗时快约 3.3 倍；另一次 35 查询本机 HTTP 测试全部低于 65 ms。代价是全库 Top-10 距离召回约 86.4%，不能声称完整精确检索。

## 实现

1. **离线长段库**：按合法起点划分 16,384 大小的长段；每个长段包含多个局部形状符号，而不是只用整个长段的一个向量。24 个学习后的特征分成四组，每组六维、每维八档，建立符号键 → 长段 ID 的倒排表，同一键在同一长段只保留一次。
2. **近似粗检索**：查询同样生成符号键，同时探测单个维度的相邻档位。按稀有度加权投票，得到候选长段。只读取命中的倒排项，不逐个扫描全库长段。常见键的倒排项仍可能很长，本版本不提供规模无关时间保证。
3. **中段筛选**：在候选长段里用已有 48 维量化摘要计算保守距离下界，按每个 1,024 起点中段的最小下界排序，选少量中段。摘要只负责筛选，不把低下界当作精确距离。
4. **点位精定位**：在选中的中段中遍历所有合法 244 点窗口，计算标准化欧氏距离，再返回 Top-10、文件、列、设备、原始 CSV 行号及匹配曲线。

长/中段按“窗口起点区间”分组，精定位从连续原始存储读取后续 243 点。因此跨段窗口不会被段边界切断。普通查询不使用 DTW。未改动原始 CSV，也保留旧精确树作为对照。

## 公平对照

同一批 70 个随机原片段查询，七类数据各十个，长度 244，种子 20260919。使用各连续段后 35% 的查询起点，模型训练使用前 55%；不添加噪声或时间变形，不剔除常量查询。每种配置使用相同查询和 FFT 全库距离对照。计时包括常驻 API、结果构造及 JSON 序列化，排除启动和网络。

| 方法（长段数/中段数） | 中位 ms | P95 ms | 最慢 ms | >100 ms | 距离 Recall@10 | 指定源位置命中 |
|---|---:|---:|---:|---:|---:|---:|
| 原精确树 | 98.37 | 275.13 | 373.13 | 35/70 | 100% | 66/70 |
| 三级 4/4 | 10.68 | 14.71 | 22.20 | 0/70 | 73.0% | 65/70 |
| **三级 12/16（默认）** | **30.06** | **38.98** | **49.46** | **0/70** | **86.43%** | **66/70** |
| 三级 24/32 | 57.01 | 78.14 | 141.83 | 1/70 | 93.0% | 66/70 |

距离 Recall 按全库第十名距离阈值计算，允许同距离并列，容差 2e-5；它不同于指定源位置命中率。原版也未返回全部指定来源，重复/常量片段使源位置不唯一。新默认配置命中总数与原版相同，不代表每个查询的返回集合相同，更不能说明所有查询均无遗漏。

默认配置在长段阶段保留真实长段 68/70，在中段阶段保留真实中段 66/70。说明除了重复片段的歧义，路由层确实存在遗漏。平均 Top-10 召回尚可，但最差查询仅 10%，须保留该限制。

精确定位的中位核验起点数：4/4 为 4,065，12/16 为 16,291，24/32 为 24,498.5。相对全库 1,023,529 个起点分别缩小约 252、63、42 倍。这只统计原始距离核验，不能忽略中段阶段的符号扫描；也不能据此宣称全链路提速同样倍数。精确树中位仅核验 734.5 个起点，但树遍历等开销更大，新方案核验较多却整体更快。

增加候选数不保证单个查询召回单调提高：新的长段会带来更多下界较低的中段，在中段配额固定时可能挤出原来的候选。表中三个配置整体平均召回提高，但不保证每条都提高。

## 常驻 HTTP 测试

独立随机种子 20260920，共 35 个原片段查询，默认 12/16，串行本机 HTTP。每次新建连接，计时包括客户端编码、网络往返、服务器计算和结果解码，不包括服务启动。

- 中位 **44.31 ms**，P95 **57.64 ms**，最慢 **64.82 ms**，超过 100 ms 为 **0/35**。
- 此 HTTP 测试验证返回协议与延迟，精度评估来自前述 70 查询对照，不混用两批样本。
- 原先 GPU `29833367508512-r1682297-n976057.csv` 的 `temperature_memory` 查询返回设备 0、起点 **85018**、终点 **85262（不含）**，距离 **0**；该次 HTTP 耗时 **63.47 ms**。
- 服务测试结束后已自动关闭测试进程。

## 正确性检查

新增测试通过：查询跨 1,024 点和 16,384 点边界、最后一个合法起点、超过百亿的 64 位逻辑起点、CSV 行号映射，以及非法查询输入。合成库中扩大到全部匹配路由候选和中段后，Top-5 与独立穷举一致。该测试验证精确定位实现，不证明近似路由不漏。

## 数据和规模边界

当前库为约 103 万原始采样点、1,023,529 合法窗口、75 个长段：项目 CSV 开发子集加一个完整 GPU CSV。未完成全部 4522 个原始 CSV 的 V2 建库，更没有在百亿点或多用户负载下验证 100 ms。

新增路由库约 66.29 MB（十进制），其中逐起点符号约 49.13 MB，倒排键、偏移、计数和 posting 等约 17.16 MB；沿用基础索引的原始存储。本原型仍保存逐起点符号，而非仅存长段级特征。构建约 2.78 秒，采用内存汇总，不能直接用于百亿点建库；需外排分片、倒排压缩及常见键处理。百亿点不得直接以本测试线性外推延迟。

接口始终返回 `certified=false` 和 `exact_distances_in_candidates=true`。精确距离只保证候选中的数值计算，无法补回被前级删除的区域。默认用候选数限制工作量，未采用强制时间截止；本次所有默认查询达标属于实测结果，不是每次 <100 ms 的硬保证。

## 复现与输出

参见 `README.md` 的三级检索命令。主要文件：

- `cascade.py`：建库、三级查询、命令行入口。
- `serve.py --cascade-index ...`：常驻 HTTP 查询。
- `benchmark_cascade.py`：同查询速度和召回对照、绘图。
- `test_cascade.py`：边界与定位测试。
- `results/cascade_benchmark/metrics.json`：全部逐查询结果。
- `results/cascade_benchmark/comparison.png`、`localization.png`：对照图和匹配曲线。
- `results/cascade_http/metrics.json`、`original_query.json`：HTTP 实测及 GPU 原查询定位。
