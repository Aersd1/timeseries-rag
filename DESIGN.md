# 百亿采样点多层检索：研究与实施规划

## 1. 已确认的目标

- 规模：100 亿个原始数值采样点；不是 100 亿条独立 244 点序列。
- 查询：从长序列任意位置截取 244 点，包括加噪、幅度/偏移变化，以及局部时间伸缩。
- 要求：多层检索、尽量低于 1 秒、可靠找回真实来源位置；保留全部有效起点。
- 数据：项目原始 CSV 与 `D:\workload\gpu`。不同列、设备和缺失值断点不能拼接。
- 新实现独立放在 `rag-search-v2`。旧后台建库已停止，已提交的旧索引保留。

## 2. 检索研究与选择

| 路线 | 可借鉴的机制 | 本项目的判断 |
|---|---|---|
| iSAX / MESSI | 树节点表示一个区域，距离下界排除整棵子树 | 正确性主干；避免扫描所有符号码 |
| SFA / SOFA | 从训练样本选择频率、学习量化边界，保留欧氏距离下界 | 用作学习型形状表示；独立实现，不宣称复现论文性能 |
| ULISSE | 相邻子序列的紧凑表示，完整保留位置覆盖 | 不复制每个 244 点原始窗口；用连续位置区间压缩 |
| DiskANN / DistributedANN | SSD 分层与分片扩展 | 适合近似候选生成，不能单独保证零漏检 |
| LLM 或端到端检索 agent | 预测去哪里搜索 | 不作为排除分支的依据；训练误差不能成为丢失真实位置的理由 |

研究来源（原始论文/作者项目页）：

1. [SOFA，ICDE 2025](https://arxiv.org/abs/2411.17483)：学习频率表示与安全距离下界。论文主要是整条序列匹配，不是本项目的全起点子序列问题。
2. [MESSI，作者项目页](https://helios2.mi.parisdescartes.fr/~themisp/messi/)：报告特定 100 GB 内存数据集上的几十毫秒查询；其硬件、数据布局与本机不同，不能直接外推。
3. [iSAX 2.0](https://www.cs.ucr.edu/~eamonn/iSAX_2.0.pdf)：大规模批量建树。
4. [ULISSE，作者项目页](https://helios2.mi.parisdescartes.fr/~themisp/ulisse/)：相邻/变长子序列的紧凑索引。
5. [DiskANN](https://www.microsoft.com/en-us/research/?p=634449)：十亿向量级 SSD 近似检索；高 Recall 不等于不漏检。
6. [DistributedANN](https://www.microsoft.com/en-us/research/wp-content/uploads/2025/07/DistributedANN_Adams_VecDBICML.pdf)：展示跨大量机器的更大规模向量检索；不能当作单机时序性能证据。

## 3. 新模型与层级结构

### 离线学习与完整覆盖

1. 从训练数据抽样窗口，学习有信息量的 Fourier 频率，以及每个特征的分位数量化边界。
2. 另外保留正交加权 PAA 特征，供 DTW 安全下界使用。
3. 对每个有效起点生成短符号码；相邻相同符号码压缩为 `[start,end)`，不删除起点。
4. 原数据按连续运行段保存一次；记录 CSV、列、设备和原始行号。
5. 批量建立分片内二叉区域树，再为分片根节点建立全局目录树。

### 在线查询

`学习型特征编码 → 全局目录树 → 分片内区域树 → 叶子符号区间 → 原始位置验证`

- 优先队列按安全下界展开，不固定只查前 16/384 个大段。
- 找到较好的候选后，用真实距离更新上界；仅在下界已大于上界时排除分支。
- 若查询设置时间预算，返回当前候选和 `certified=false`；不能声称已经查全。
- 无预算的精确模式必须继续搜索，直到剩余分支都已安全排除。
- 对纯常数连续段可以直接计算共同距离，但仍保留其位置区间。

## 4. 距离与不漏检的边界

### ED：加噪、幅度和偏移

对窗口分别做 z-normalization。选中的实值正交 Fourier 系数是投影，其距离不大于完整形状距离。加权 PAA 也具有此性质。

量化树存储特征的包围区间；查询特征到区间的距离是有效下界。Fourier 和 PAA 投影可能重叠，两个下界取最大值，不能直接相加。

### DTW：有限时间伸缩

使用固定 Sakoe–Chiba 半径的 z-normalized DTW。先由查询构造逐点上、下包络，再计算其 PAA 区间。候选 PAA 到包络 PAA 的距离不大于逐点 LB_Keogh，后者不大于该半径下的 DTW 距离。

DTW 模式不使用 ED 的 Fourier 下界剪枝。叶子用逐点 LB_Keogh 及带提前放弃的原生 DTW 验证。

### 不能混淆的保证

- 索引完整性：所有有效起点都在索引中。
- 距离正确性：精确模式找到指定度量下的真正 Top-k（浮点容差内）。
- 来源恢复：被加噪/变形的查询是否仍返回其生成来源。这是需要独立实测的指标；变形过大或有重复数据时不能由距离正确性自动推出。
- 1 秒：性能目标，不能在任意数据、硬件与并发负载下作硬实时承诺。不能以静默漏检换取“达标”。

## 5. 百亿点容量规划

- 100 亿 float64 原始点约 80 GB；float32 为 40 GB，但原值保真模式保留 float64。
- 若每个有效起点保存 32 个 SFA 字节 + 16 个 PAA 字节，最坏约 480 GB 特征，另需位置和树结构。
- 相邻区间压缩的实际收益必须从真实数据测量，不能预设压缩倍数。
- 索引文件采用 mmap 和 64 位位置；分片可独立构建，内存不随整个语料线性常驻。
- 本机先做正确性与可测规模验证；输出基于实际记录压缩率的百亿点容量测算。单机验证不冒充百亿点实测，分片格式不冒充已部署分布式集群。

## 6. 实施顺序与验收

1. 实现训练模型、原生滑动特征与距离内核。
2. 实现无损位置区间、两级树目录、持久化、精确/预算查询。
3. 用随机与高频、常数、近常数、跨边界、缺失/交错设备数据对照直接穷举。
4. 独立划分训练/验证查询，测试原片段、5%/10% 噪声、幅度偏移、有限时间变形。
5. 报告实际数据规模、来源 Recall、Top-k Recall、认证比例、p50/p95/最大延迟、读取与精算数量、建库时间、索引大小。
6. 给出 CSV 全量建库入口与容量规划工具。只有完成相应规模建库并查询后才能声称该规模达标。
