# 普通检索 100 ms 测试

## 结论

已停止针对时间变形优化，改测 244 点原始片段的标准化欧氏距离检索。加入引导叶候选初始化、常驻元数据加载和限时服务接口。当前只在本轮 35 个普通查询的精确 Top-1 上观察到全部低于 100 ms；完整 Top-10 未满足要求。没有完成百亿原始点性能验证。

## 最新结果

数据：1,023,529 个合法子序列起点，约 103 万原始点。七类数据各随机取五段，随机种子 20260918；不加噪、不变形，不剔除常量查询。查询从各连续段后 35% 取样，模型训练使用前 55%。计时包括常驻 Python API、结果构造和 JSON 序列化，不包括启动、网络和客户端展示。搜索期间有操作系统调度波动；未进行实时调度隔离。

| 方法 | 中位 ms | P95 ms | 最慢 ms | 超过 100 ms | 距离 Recall@k | 完成精确搜索 |
|---|---:|---:|---:|---:|---:|---:|
| 精确 Top-1 | 17.78 | 35.05 | 66.69 | 0/35 | 100% | 35/35 |
| 精确 Top-10 | 204.36 | 1177.49 | 1543.94 | 26/35 | 100% | 35/35 |
| 80 ms 预算 Top-10 | 83.48 | 121.92 | 206.96 | 5/35 | 88.57% | 8/35 |

距离召回以全量 FFT 检索结果的第 k 名距离阈值为对照，允许同距离并列，数值容差 2e-5。此前三项单元测试还使用独立穷举与 DTW 动态规划核验下界和树搜索。本次完整测试通过。

三种模式的指定源位置命中均为 31/35；返回同距离片段不代表返回了指定源位置。重复/常量片段存在来源歧义，不能以距离 Recall=100% 声称指定源位置全部找回。本轮只测试原片段，不证明加噪查询性能。

## 为什么预算不能保证每次达标

预算检查发生在算法检查点，线程被系统暂停、内存映射缺页和结果序列化仍会消耗时间。到达预算时返回未完成结果，完整 Top-10 的召回会下降。本次最差限时查询的距离 Recall@10 只有 10%。接口用 `certified=false` 标识未完成，不能把它包装成精确结果。

Top-1 找到零距离即可证明最优；Top-10 还必须寻找和证明其余九个近邻，因此两者开销差异很大。用户原目标包含极相似结果，不能未经同意将默认需求缩减成 Top-1。

## 百亿点仍需完成的工作

当前全局目录和分片树对部分查询剪枝不足。下一阶段需改善分片聚类、紧致下界和常驻执行路径，再用固定测试集对照；若使用 ANN 近似路由，必须报告召回损失。分布式服务原型尚未经过真实集群压测。全量 CSV 尚未建成此 V2 索引。此前容量脚本对 96 维配置的推算为约 975 GB 完成索引、1.79 TB 建库峰值，本机空间不支持直接完成该规模；普通检索使用的是更小的 48 维配置，两种容量不可混用。

因此当前交付是可复现的新实现和未达标证据，不是百亿点、完整 Top-10、每次 100 ms 的已完成系统。

## 文件

- `benchmark_100ms.py`：普通查询速度、精度、图表生成代码。
- `results/ordinary_final_100ms/metrics.json`：逐条记录、配置、数据范围。
- `results/ordinary_final_100ms/latency.png`：延迟分布和召回/完成率。
- `index.py`：引导候选与保守下界精确检索。
- `README.md`：常驻服务及复现实验命令。
