# 完整召回改进：候选检索 + 安全补查

## 结论

已将三级检索的默认行为改为完整召回模式。当前完成索引上的 70 个随机查询，以及独立的 35 个 HTTP 查询，逐条 Top-10 距离召回均为 100%。同批近似模式的平均召回仍为 86.43%，因此提升来自补查，不是更换了测试集。

完整召回并未同时实现“所有请求均低于 100 ms”：70 次常驻 API 查询中有 3 次超时，35 次 HTTP 查询也有 3 次超时。当前没有百亿点验证。

## 为什么可以补齐

原来的长段和中段数量上限会直接丢掉区域，即使被丢弃区域中有真正的近邻，最后的精确定位也无法恢复它。

现在分两步：

1. 保留近似三级检索，快速拿到 k 个真实候选及其精确距离。第 k 名距离记为 τ，它提供一个可行的距离上限。
2. 检查全库符号摘要。对每个位置计算不大于真实距离的下界 L。只有 L > τ 时才安全跳过，否则读取原始数据核验并更新 Top-k。当前 k 个结果都是零距离时可以提前结束，因为任何 k 个零距离并列结果都已最优。

随着发现更近候选，τ 只会下降。被安全排除的位置无法进入最终 Top-k；无法排除的位置都经过精确计算。中段摘要和候选配额只决定初始搜索顺序，不再决定最终检索范围。保证范围是已完成索引、244 点窗口、标准化欧氏距离，以及实现注明的浮点容差和保守下界，不包括 DTW、未建库 CSV 或任意来源唯一性。

实现了两种补查：`tree` 用原精确树继续搜索；`scan` 用优化后的全库符号检查。实测 `scan` 更快，因此设为默认。它是全局符号扫描，不是扫描所有原始窗口，亦不能声称复杂度与数据库大小无关。

## 性能优化

- 每次查询预计算“每个特征/每个符号档位”的距离下界查表；符号累加一旦超过 τ 就提前跳过该位置。
- 启动时缓存每个窗口的均值和标准差。精确核验保持独立穷举相同的窗口局部中心化算法，避免每次重新计算 244 点统计量。
- 常量窗口直接计算距离，不反复遍历同样的常量值。
- 原始距离只计算在符号下界无法排除的位置上，候选重复位置合并。

统计量缓存增加 16 字节/起点，当前约 16.38 MB。缓存准备属于启动过程，不计入常驻查询时间。这是空间换查询时间；百亿起点对应额外约 160 GB，不能视为免费优化。

## 同查询对照：70 个原片段

种子 20260919，七类数据各十个查询，长度 244，取各连续段后 35% 的起点，不添加噪声、不变形、不剔除常量。索引为 1,023,529 个合法起点，约 103 万原始采样点。每个模式使用同一批查询，FFT 全库结果提供距离对照，原精确树也逐条核验通过。

| 方法 | 中位 ms | P95 ms | 最慢 ms | >100 ms | 平均/最差 Recall@10 |
|---|---:|---:|---:|---:|---:|
| 原精确树 | 129.16 | 412.19 | 777.88 | 42/70 | 100% / 100% |
| 三级近似，无补查 | 33.47 | 56.09 | 64.42 | 0/70 | 86.43% / 10% |
| 三级候选 + 精确树补查 | 153.48 | 449.94 | 764.47 | 49/70 | 100% / 100% |
| **三级候选 + 缓存符号补查（默认）** | **43.00** | **95.65** | **223.95** | **3/70** | **100% / 100%** |

计时包括常驻 API、结果生成、JSON 序列化；不包括进程启动与网络。所有补查模式均完成搜索并返回 `certified=true`。没有按时间截断后仍标为完整的结果。设备和操作系统调度存在波动，应使用本批同查询对照，不应与此前其他批次直接相除计算提速。

未缓存统计量的第一轮符号补查，中位 50.44 ms、最慢 545.84 ms，均保持 100% 召回；缓存后最慢耗时明显降低。两次独立运行有系统波动，这不是隔离 CPU 条件下的严格消融实验。

图中的真实长段/中段命中率指初始近似阶段，仍可能不足 100%；绿色距离召回展示的是补查后的最终结果，二者不要混淆。

## 独立 HTTP 查询：35 个原片段

种子 20260920。单客户端、逐次建立本机 HTTP 连接，默认 `scan`；计时包含客户端编码、网络往返、服务端执行和客户端解码，排除服务启动。每条响应另外与 FFT 对照，均为 100% Recall@10。

- 中位 **56.71 ms**，P95 **101.50 ms**，最慢 **105.66 ms**。
- **3/35** 超过 100 ms。
- GPU 旧查询返回 `temperature_memory`、设备 0、原始起点 **85018**、终点 **85262（不含）**，距离 **0**；该次 HTTP 耗时 **92.73 ms**，搜索完整。

## 100% 的含义

这里的 100% 是固定距离定义下的全库 Top-10 距离召回，允许同距离并列。测量容差为 2e-5，符号下界每维有 1e-4 向外扩展，极小距离按原实现容差归零。不能把数值工程保证说成对任意浮点输入的数学精确证明。

指定源位置命中仍为 **66/70**，原精确树也是 66/70。重复/常量片段可能在许多位置距离相同，固定十个返回位置无法保证包含某个指定来源。要列出所有同距离来源，应另做不限制为十条的并列结果/范围查询；本次没有把“指定来源 100%”混同于“距离召回 100%”。

测试数据仍是项目开发子集和一个完整 GPU CSV，未覆盖全部 4522 个原始文件。全局符号补查最坏需要遍历所有符号，百亿点需要进一步分片及紧致区域下界设计；不具备已验证的百亿点 100 ms 保证。

## 验证与使用

五项回归测试全部通过，包括：符号下界、滑动编码、完整树与穷举对照、跨中/长段边界、末尾合法起点、64 位大位置、随机非原片段查询，以及故意只保留 1 个长段和 1 个中段后的完整补查。新增缓存核验还覆盖常量与 1e12 偏置小波动数据，缓存算法和直接计算的有效候选距离一致。

默认已开启补查，无需重新建符号库：

```powershell
C:\Python314\python.exe cascade.py query --index results/cascade_index --query-json ../rag-search/results/full_corpus/query.json --verification scan
C:\Python314\python.exe serve.py --index results/validation_index --cascade-index results/cascade_index --port 8770
```

HTTP 请求可显式加入 `"verification":"scan"`。`"none"` 为旧近似模式，`"tree"` 为精确树补查模式。完整模式不执行时间预算强制截断；所有模式的启动均需准备缓存。

主要输出：

- `results/cascade_certified_cached/metrics.json`：最新 70 查询完整逐条记录。
- `results/cascade_certified_cached/comparison.png`：速度和召回对照图。
- `results/cascade_certified_http/metrics.json`：35 查询 HTTP 测试和召回。
- `results/cascade_certified_http/original_query.json`：GPU 原查询的完整返回。
- `cascade.py`、`native.c`、`kernels.py`：补查与缓存实现。
- `test_cascade.py`、`test_index.py`：回归测试。
